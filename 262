path="$1"

case "$(uname -s)" in
  Darwin) default_jobs=60 ;;
  *)      default_jobs=0 ;;
esac

JOBS="${JOBS:-$default_jobs}"

find "$path" -name '*.js' ! -name '*_FIXTURE.js' -print0 |
    sort -z |
    xargs -0 ./target/release/test262 --jobs 100
